import { JournalListPipe } from './journal-list.pipe';

describe('JournalListPipe', () => {
  it('create an instance', () => {
    const pipe = new JournalListPipe();
    expect(pipe).toBeTruthy();
  });
});
