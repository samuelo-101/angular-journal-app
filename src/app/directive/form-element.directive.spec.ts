import { FormElementDirective } from './form-element.directive';

describe('FormElementDirective', () => {
  it('should create an instance', () => {
    const directive = new FormElementDirective();
    expect(directive).toBeTruthy();
  });
});
